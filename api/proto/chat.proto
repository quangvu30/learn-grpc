syntax = "proto3";
package chat;
option go_package = "github.com/quangvu30/learn-grpc/pkg/api";
message Message {
    string body = 1;
}

service ChatService {
    rpc SayHello(Message) returns (Message) {}
}